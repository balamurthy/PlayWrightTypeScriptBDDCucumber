# PlayWrightTypeScriptBDDCucumber

Cucumber Playwright TypeScript 
Create a folder
Open with VsCode
Navigate to the folder in the terminal
npm init playwright@latest –yes -- --quiet –browser=chromium 

Go to plugins and install Playwright Test for VSCode
Delete the playwright config ts file and sample test
Install Cucumber plugin
npm i @cucumber/cucumber -D
npm i ts-node -D
In VSCode , create folders src/test/features and src/test/steps

Website
bookcart.azurewebsites.net

cucumber.json should be created in project root folder

Create loginsteps.ts file inside steps folder.
Paste the snippet generated after npm test
Ref. project config files and code in the repo.
